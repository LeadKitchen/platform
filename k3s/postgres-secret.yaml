# Template only — every value below is a placeholder. Fill it in with
# `kubectl edit secret postgres-secret -n sitruk` (or apply your own copy)
# and never commit the real value.
#
# The same password must be reflected in app-secret.yaml's POSTGRES_URL,
# e.g. postgres://postgres:<password>@postgres:5432/acme
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: sitruk
type: Opaque
stringData:
  POSTGRES_PASSWORD: ""
