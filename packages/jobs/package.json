{
  "name": "@acme/jobs",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./src/index.ts"
    }
  },
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "bun --watch src/worker.ts",
    "worker": "bun src/worker.ts",
    "format": "biome format .",
    "lint": "biome lint .",
    "check": "biome check .",
    "reindex:global-knowledge": "bun --env-file=../../.env run src/scripts/reindex-global-knowledge.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "dependencies": {
    "@acme/ai": "workspace:*",
    "@acme/config": "workspace:*",
    "@acme/db": "workspace:*",
    "@acme/storage": "workspace:*",
    "@hatchet-dev/typescript-sdk": "^1.31.1",
    "mammoth": "^1.12.2",
    "unpdf": "^1.8.1",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@acme/tsconfig": "workspace:*",
    "@types/node": "26.4.1",
    "typescript": "7.0.2"
  }
}
